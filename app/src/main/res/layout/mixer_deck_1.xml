<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight=".25"
    android:orientation="vertical"
    android:paddingBottom="8dp"
    android:paddingEnd="0dp"
    android:paddingStart="8dp"
    android:paddingTop="8dp"
    tools:showIn="@layout/mixer">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/secondary_text"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <com.triggertrap.seekarc.SeekArc
            android:id="@+id/deckOneGain"
            android:layout_width="@dimen/seekArcSize"
            android:layout_height="@dimen/seekArcSize"
            android:layout_margin="@dimen/seekArcMargin"
            android:padding="@dimen/seekArcPadding"/>

        <com.triggertrap.seekarc.SeekArc
            android:id="@+id/deckOneFilter"
            android:layout_width="@dimen/seekArcSize"
            android:layout_height="@dimen/seekArcSize"
            android:layout_margin="@dimen/seekArcMargin"
            android:padding="@dimen/seekArcPadding"/>
    </LinearLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:background="@color/secondary_text">

        <SeekBar
            android:id="@+id/deckOneVolume"
            style="@style/MidiControlVerticalSeekbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"/>
    </RelativeLayout>
</LinearLayout>

    